{
  "name": "simple-obs-root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/Brendonovich/simple-obs"
  },
  "scripts": {
    "test": "yarn jest --runInBand",
    "test:coverage": "yarn jest --runInBand --coverage"
  }
}
